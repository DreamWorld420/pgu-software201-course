import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
	colors: {
		_black: "#110E1B",
		_primary: "#3C162F",
		_secondary: "#5C162E",
		_white: "#fafafa",
	},
});

export default theme;
